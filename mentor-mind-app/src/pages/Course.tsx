import { Grid } from "@mui/material";
import Header from "../components/bar/Header";

const Course = () => {
  return (
    <Grid
      sx={{
        backgroundColor: "red",
        marginTop: "100px",
        width: "100px",
        height: "100px",
      }}
    ></Grid>
  );
};
export default Course;
